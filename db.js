/**
 * @module team14/db
 */

'use strict';

var Database = {
  connect: connect
};

/**
 * Connects to a Google Sheet Database
 * @param {function} cb
 */
function connect(cb) {
  // connect to our database... somehow
  cb();
};

module.exports = Database;
